import Placeholder from '@/features/misc/Placeholder';

export default function SettingsPage() {
    return <Placeholder title="Settings" note="Event settings, gates, sessions, roles and users, and the audit log viewer." />;
}
