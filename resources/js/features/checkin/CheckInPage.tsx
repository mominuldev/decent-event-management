import Placeholder from '@/features/misc/Placeholder';

export default function CheckInPage() {
    return <Placeholder title="Check-in" note="Live gate monitor, scan log, override requests, and device/volunteer status." />;
}
